# Module 010: Quiz

## Question 1 (Multiple Choice)
Why must `input()` be converted to a number before doing math?
- A) `input()` always returns a string
- B) `input()` returns a float automatically
- C) `input()` returns an int automatically
- D) No conversion is needed

## Question 2 (What would this output?)
```python
print("5" + "3")
```

## Question 3 (Multiple Choice)
What should you check before performing division?
- A) That the operator is `+`
- B) That the second number is not zero
- C) That both numbers are integers
- D) That the first number is larger

## Question 4 (Multiple Choice)
What operator computes the remainder of division?
- A) `//`
- B) `/`
- C) `%`
- D) `**`

## Question 5 (What does this calculator output?)
- Input: `num1 = 10`, `operator = "**"`, `num2 = 3`

## Question 6 (Multiple Choice)
What happens in the reference solution when the user types "done" as the first number?
- A) The program crashes
- B) The program prints "Goodbye!" and exits
- C) The program asks for a number again
- D) The program resets

## Question 7 (Short Answer)
Name two operators that require a division-by-zero check.

## Question 8 (Multiple Choice)
In the reference solution, what type is used to convert user input?
- A) `int()`
- B) `float()`
- C) `str()`
- D) `bool()`

---

## Answers

1. **A** — `input()` always returns a string
2. **`53`** (string concatenation, not numeric addition)
3. **B** — That the second number is not zero
4. **C** — `%`
5. **10.0 ** 3.0 = 1000.0** (or similar float representation)
6. **B** — The program prints "Goodbye!" and exits
7. **`/`, `//`, and `%`** (all three involve division)
8. **B** — `float()` for general-purpose use
