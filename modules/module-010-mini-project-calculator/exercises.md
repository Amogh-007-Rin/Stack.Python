# Module 010: Exercises

## Exercise 1: Manual Calculation
Trace through this code manually. What is the output?
```python
num1 = 15.0
op = "//"
num2 = 4.0
if op == "//":
    result = num1 // num2
print(result)
```

## Exercise 2: Division by Zero
Write a code snippet that checks if the user has entered zero as the second number and prints "Cannot divide by zero" instead of crashing.

## Exercise 3: Input Validation
Why should you convert `input()` to `float()` instead of `int()` for a general-purpose calculator?

## Exercise 4: Operator Check
Write code that checks if a variable `op` is one of the seven valid operators. If not, print "Invalid operator".

## Exercise 5: Calculator Output
Given `num1 = 7`, `num2 = 3`, and `op = "/"`, what would the calculator display?

## Exercise 6: Edge Cases
List three edge cases you should test your calculator with.

## Exercise 7: Formatting
Modify the calculator to display results with exactly 2 decimal places for division operations.

## Exercise 8: Loop Design
Describe in 3-4 steps the flow of the calculator's main loop (from start to exit).
