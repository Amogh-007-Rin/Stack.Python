# Quiz: Introduction to SQL from Python

## Question 1
Which SQL clause is used to filter rows after aggregation?
- A) `WHERE`
- B) `HAVING`
- C) `FILTER`
- D) `LIMIT`

## Question 2
Which JOIN returns all rows from the left table and matching rows from the right?
- A) INNER JOIN
- B) RIGHT JOIN
- C) LEFT JOIN
- D) FULL JOIN

## Question 3
Which function returns the number of rows in a group?
- A) `TOTAL()`
- B) `COUNT()`
- C) `SUM()`
- D) `LENGTH()`

## Question 4
What does `row_factory = sqlite3.Row` enable?
- A) Faster queries
- B) Column access by name
- C) Automatic indexing
- D) Type conversion

## Question 5
Which method fetches the next row as a single tuple?
- A) `cursor.fetchone()`
- B) `cursor.fetchfirst()`
- C) `cursor.get()`
- D) `cursor.next()`

## Answer Key
1-B, 2-C, 3-B, 4-B, 5-A
