# Quiz: Concurrency: Threading

## Question 1
What does `thread.join()` do?
- A) Creates a new thread
- B) Waits for the thread to finish
- C) Kills the thread
- D) Puts the thread to sleep

## Question 2
What is the GIL?
- A) A lock that prevents multiple threads from executing Python bytecode simultaneously
- B) A tool for debugging threads
- C) A global variable registry
- D) A type of garbage collector

## Question 3
Which module provides thread-safe data structures for communication between threads?
- A) `threading`
- B) `queue`
- C) `multiprocessing`
- D) `asyncio`

## Question 4
What is a daemon thread?
- A) A thread that runs as a background process
- B) A thread that automatically terminates when the main thread exits
- C) A thread with higher priority
- D) A thread that cannot be joined

## Question 5
What is the key difference between `Lock` and `RLock`?
- A) `Lock` is faster
- B) `RLock` can be acquired multiple times by the same thread without deadlock
- C) `RLock` can only be used in daemon threads
- D) There is no difference

## Answer Key
1-B, 2-A, 3-B, 4-B, 5-B
