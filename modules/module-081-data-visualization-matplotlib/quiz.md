# Quiz: Data Visualization with matplotlib

## Question 1
Which function creates a new figure with a grid of subplots?
- A) `plt.figure()`
- B) `plt.subplots()`
- C) `plt.subplot()`
- D) `plt.grid()`

## Question 2
How do you save the current figure to a file?
- A) `plt.save()`
- B) `plt.export()`
- C) `plt.savefig()`
- D) `plt.write()`

## Question 3
Which parameter in `plt.plot` controls the marker style?
- A) `color`
- B) `linestyle`
- C) `marker`
- D) `style`

## Question 4
What does `plt.barh` create?
- A) A vertical bar chart
- B) A horizontal bar chart
- C) A stacked bar chart
- D) A histogram

## Question 5
Which function is used to display a legend on a plot?
- A) `plt.legend()`
- B) `plt.label()`
- C) `plt.key()`
- D) `plt.show()`

## Answer Key
1-B, 2-C, 3-C, 4-B, 5-A
