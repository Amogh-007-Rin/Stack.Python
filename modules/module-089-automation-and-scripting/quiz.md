# Quiz: Automation & Scripting (shutil, Scheduling, File Automation)

## Question 1
Which shutil function creates a ZIP archive?
- A) `shutil.zip()`
- B) `shutil.make_archive()`
- C) `shutil.compress()`
- D) `shutil.pack()`

## Question 2
What does `glob.glob('**/*.py', recursive=True)` return?
- A) All .py files in the current directory
- B) All .py files in the entire directory tree
- C) All files with 'py' in their name
- D) All Python processes

## Question 3
Which function removes a directory and all its contents?
- A) `os.rmdir()`
- B) `shutil.rmtree()`
- C) `os.remove()`
- D) `shutil.cleandir()`

## Question 4
What does `os.walk` yield?
- A) A list of all files
- B) Tuples of (dirpath, dirnames, filenames)
- C) A flat list of paths
- D) A dictionary of file metadata

## Question 5
Which library is used for in-process task scheduling?
- A) `cron`
- B) `schedule`
- C) `timer`
- D) `at`

## Answer Key
1-B, 2-B, 3-B, 4-B, 5-B
