# Quiz: Data Cleaning & Analysis with pandas

## Question 1
Which method removes rows with missing values?
- A) `.fillna()`
- B) `.dropna()`
- C) `.remove_na()`
- D) `.delete_na()`

## Question 2
How do you check for missing values in a DataFrame?
- A) `.check_null()`
- B) `.has_null()`
- C) `.isnull()`
- D) `.missing()`

## Question 3
Which method is used to remove duplicate rows?
- A) `.remove_duplicates()`
- B) `.dedup()`
- C) `.drop_duplicates()`
- D) `.unique()`

## Question 4
What does `df.groupby('col')['val'].mean()` return?
- A) A DataFrame with all columns
- B) A Series with mean values per group
- C) A list of mean values
- D) A pivot table

## Question 5
Which function merges two DataFrames on a common column?
- A) `pd.concat()`
- B) `pd.merge()`
- C) `pd.join()`
- D) `pd.combine()`

## Answer Key
1-B, 2-C, 3-C, 4-B, 5-B
