# Quiz: Introduction to Data with pandas

## Question 1
What is a pandas Series?
- A) A two-dimensional labeled data structure
- B) A one-dimensional labeled array
- C) A three-dimensional data structure
- D) A database table

## Question 2
Which method shows the first few rows of a DataFrame?
- A) `.show()`
- B) `.first()`
- C) `.head()`
- D) `.top()`

## Question 3
How do you select a single column as a DataFrame?
- A) `df['col']`
- B) `df[['col']]`
- C) `df.col`
- D) `df.get('col')`

## Question 4
Which method provides summary statistics of numeric columns?
- A) `.summary()`
- B) `.stats()`
- C) `.describe()`
- D) `.info()`

## Question 5
What does `df[df['Age'] > 30]` return?
- A) A DataFrame with only the Age column where Age > 30
- B) A DataFrame filtered to rows where Age > 30
- C) A boolean Series
- D) A list of ages greater than 30

## Answer Key
1-B, 2-C, 3-B, 4-C, 5-B
