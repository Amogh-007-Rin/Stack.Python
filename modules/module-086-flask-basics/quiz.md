# Quiz: Introduction to Web Development: Flask Basics

## Question 1
Which decorator is used to define a route in Flask?
- A) `@app.endpoint()`
- B) `@app.route()`
- C) `@app.url()`
- D) `@app.path()`

## Question 2
How do you access form data in Flask?
- A) `request.data`
- B) `request.args`
- C) `request.form`
- D) `request.body`

## Question 3
Which function redirects to another route?
- A) `redirect()`
- B) `forward()`
- C) `transfer()`
- D) `navigate()`

## Question 4
What does `url_for('profile', name='Alice')` generate?
- A) A dictionary of URL parts
- B) The URL for the `profile` route with parameter name=Alice
- C) A form action URL
- D) An absolute file path

## Question 5
Which method returns a JSON response in Flask?
- A) `response.json()`
- B) `jsonify()`
- C) `json.dumps()`
- D) `render_json()`

## Answer Key
1-B, 2-C, 3-A, 4-B, 5-B
