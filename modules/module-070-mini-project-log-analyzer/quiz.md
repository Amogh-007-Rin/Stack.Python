# Module 070: Milestone Project: Log File Analyzer — Quiz

1. **Which module is used to access command-line arguments in the project?**
   - a) `os`
   - b) `argparse`
   - c) `sys`
   - d) `getopt`

2. **What regular expression pattern would capture a typical log level like INFO or ERROR?**
   - a) `[A-Z]+`
   - b) `\d+`
   - c) `\w+@`
   - d) `\s+`

3. **Which data structure is best for counting log entries by severity level?**
   - a) `list`
   - b) `set`
   - c) `dict` or `collections.Counter`
   - d) `tuple`

4. **What does filtering by date range require about the log format?**
   - a) The log must be in JSON format
   - b) Each entry must contain a parseable timestamp
   - c) The log file must be sorted
   - d) Date ranges are not supported

5. **Which output format could the project use for exporting results?**
   - a) JSON
   - b) CSV
   - c) Text report
   - d) All of the above

<details>
<summary>Answers</summary>
1-c, 2-a, 3-c, 4-b, 5-d
</details>
