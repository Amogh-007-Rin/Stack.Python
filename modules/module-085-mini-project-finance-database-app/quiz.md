# Quiz: Milestone Project: Personal Finance Database App

## Question 1
Which SQLAlchemy function is used to group by a column and sum amounts?
- A) `func.groupby()` and `func.total()`
- B) `func.group_by()` and `func.sum()`
- C) `session.query().group_by()` and `func.sum()`
- D) `session.aggregate().group()`

## Question 2
In the CLI menu loop, which statement exits the loop?
- A) `exit()`
- B) `break`
- C) `return`
- D) `stop`

## Question 3
What does `Transaction.amount > 0` typically represent?
- A) An expense
- B) An income
- C) A refund
- D) An error

## Question 4
Which method creates all database tables from model definitions?
- A) `Base.create_all(engine)`
- B) `Base.metadata.create_all(engine)`
- C) `engine.create_tables()`
- D) `session.create_all()`

## Question 5
What does `func.strftime('%Y-%m', Transaction.date)` do?
- A) Converts date to timestamp
- B) Extracts year-month as a string
- C) Formats the date for display
- D) Calculates date difference

## Answer Key
1-C, 2-B, 3-B, 4-B, 5-B
