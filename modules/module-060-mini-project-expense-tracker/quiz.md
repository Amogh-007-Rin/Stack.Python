# Module 060: Milestone Project — Quiz

1. **What is the primary data format for persistence in this project?**
   - a) JSON
   - b) CSV
   - c) SQLite
   - d) YAML

2. **Which Python module is used for CSV operations?**
   - a) `json`
   - b) `csv`
   - c) `pickle`
   - d) `text`

3. **Why does `Expense` use `@dataclass`?**
   - a) It's required for CSV serialization
   - b) It auto-generates `__init__`, `__repr__`, `__eq__`
   - c) It's faster
   - d) It allows inheritance

4. **What does the `month` property return?**
   - a) The full month name
   - b) `YYYY-MM` format
   - c) The month number only
   - d) A `datetime` object

5. **Which method creates the CSV file with headers?**
   - a) `add_expense`
   - b) `__init__` of ExpenseTracker
   - c) `export_summary`
   - d) The main CLI function

<details>
<summary>Answers</summary>
1-b, 2-b, 3-b, 4-b, 5-b
</details>
