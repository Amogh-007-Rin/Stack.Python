# Quiz: Weather Dashboard CLI

## Question 1
Which HTTP status code indicates "City not found" when calling OpenWeatherMap API?
- A) 401
- B) 404
- C) 500
- D) 403

## Question 2
Which argparse method adds a positional argument?
- A) `parser.add_argument()`
- B) `parser.add_option()`
- C) `parser.positional()`
- D) `parser.arg()`

## Question 3
How would you handle a timeout in a requests call?
- A) `requests.get(url, timeout=10)`
- B) `requests.get(url, max_retries=10)`
- C) `requests.get(url, wait=10)`
- D) `requests.get(url, delay=10)`

## Question 4
What is the purpose of `response.raise_for_status()`?
- A) To print the response status code
- B) To raise an exception for HTTP errors
- C) To raise the response priority
- D) To check if the response is cached

## Question 5
In the project, what is the recommended format to store recent searches?
- A) CSV
- B) JSON
- C) XML
- D) SQLite

## Answer Key
1-B, 2-A, 3-A, 4-B, 5-B
