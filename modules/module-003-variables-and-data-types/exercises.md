# Module 003: Exercises

## Exercise 1: Create Variables
Create three variables: `city` (string), `population` (int), and `area_km2` (float). Assign them values and print each one.

## Exercise 2: Variable in Expression
Assign `a = 15` and `b = 4`. Print the result of `a + b`, `a * b`, and `a / b`.

## Exercise 3: Dynamic Typing Demo
Create a variable `thing` and assign it an integer. Print the type. Then reassign it to a string and print the type again.

## Exercise 4: Check Your Knowledge
What is the output of each `print()` call?
```python
x = True
print(type(x))
y = "42"
print(type(y))
z = 42.0
print(type(z))
```

## Exercise 5: Invalid Names
Which of the following are valid variable names? Correct the invalid ones.
- `2nd_place`
- `my_var`
- `class`
- `_data`
- `user-name`
- `total$

## Exercise 6: Case Sensitivity
What will this code print?
```python
name = "Alice"
Name = "Bob"
print(name)
```

## Exercise 7: Profile
Create variables for your own profile: first name, last name, year of birth, height in meters, and whether you have used Python before. Print each value.

## Exercise 8: Type Exploration
What type is the result of `10 / 2`? Write code to check.
