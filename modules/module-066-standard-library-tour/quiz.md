# Module 066: The Python Standard Library Tour — Quiz

1. **Which `os` function returns the current working directory?**
   - a) `os.cwd()`
   - b) `os.getcwd()`
   - c) `os.pwd()`
   - d) `os.curdir()`

2. **Which `sys` attribute holds command-line arguments?**
   - a) `sys.args`
   - b) `sys.argv`
   - c) `sys.arguments`
   - d) `sys.cmdline`

3. **What does `datetime.datetime.now()` return?**
   - a) A `date` object
   - b) A `time` object
   - c) A `datetime` object
   - d) A string

4. **Which `random` function returns a random integer from 1 to 10 inclusive?**
   - a) `random.random(1, 10)`
   - b) `random.randint(1, 10)`
   - c) `random.choice(1, 10)`
   - d) `random.uniform(1, 10)`

5. **How do you join path components portably?**
   - a) `os.path.join()`
   - b) `os.joinpath()`
   - c) `os.path.concat()`
   - d) `os.combine()`

<details>
<summary>Answers</summary>
1-b, 2-b, 3-c, 4-b, 5-a
</details>
